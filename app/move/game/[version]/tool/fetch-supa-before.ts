// import { createClient } from '@/app/utils/supabase/server';

// export default async function fetchSupaBefore() {
//   const supabase = await createClient();

//   const { data, error } = await supabase.from('version_move').select('*');

//   if (error) {
//     return [];
//   }

//   return data;
// }
